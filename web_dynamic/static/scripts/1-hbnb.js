#!/usr/bin/node

window.onload = function () {
    const checkbox = document.querySelector('input[type=checkbox]')
    checkbox.addEventListener('change', function() {
        if (this.checked) {
            const a_li = document.querySelector("amenities li");
            const a_id = a_li.getAttribute(':amenity_id');
            const dir_am = {'amenity_id'}}
        }
    ) }